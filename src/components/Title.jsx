import React from 'react';
import PropTypes from 'prop-types';

const Title = (props) => {
  return (
    <div>
      <h1>MY RTL PROJECT</h1>
    </div>
  );
};

Title.propTypes = {};

export default Title;
